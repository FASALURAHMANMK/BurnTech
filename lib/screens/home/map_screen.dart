import 'package:flutter/material.dart';

class MapTab extends StatelessWidget {
  const MapTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Placeholder Map Tab
    return Container(
      color: Colors.white,
      child: const Center(child: Text('Map Tab (Under Development)')),
    );
  }
}